# Runtime and test dependencies. Lower bounds only: newer releases are expected
# to work, and pip resolves a build appropriate to the interpreter on 3.10-3.14.

# timing_engine draws from np.random.Generator/PCG64. It is the only
# third-party import in the package.
numpy>=1.24.0

pytest>=7.4.0
