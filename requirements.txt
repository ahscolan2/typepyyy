# Runtime dependencies. Lower bounds only: newer releases are expected to work,
# and pip resolves a build appropriate to the interpreter on 3.10-3.14.

# timing_engine draws from np.random.Generator/PCG64. It is the only
# third-party import in the package.
numpy>=1.24.0

# Test and lint tools are NOT here: installing from this file should not put a
# test runner in a runtime environment. See requirements-dev.txt, or
# `pip install -e ".[dev]"`.

# Optional emitter dependencies, used only by "python main.py --emit ...".
# The generator itself imports neither; uncomment only if you use --emit.
# playwright>=1.40   # --emit docs; also run: playwright install chromium
# pynput>=1.7        # --emit desktop
